language: ruby
script: rspec spec --format documentation --order random

install:
  - gem install bundler -v 1.17.3
  - gem update bundler
  - bundle install

rvm:
  - 2.5.7
  - 2.6.4
  - 2.7.0

branches:
  only:
    - master
    - develop

