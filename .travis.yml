language: ruby
script: rspec spec --format documentation --order random

install:
  - gem update bundler
  - bundle install

rvm:
  - 2.4.9
  - 2.5.7
  - 2.6.4
  - 2.7.0

branches:
  only:
    - master
    - develop

